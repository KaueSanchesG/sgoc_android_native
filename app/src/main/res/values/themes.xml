<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SistemaDeGerenciamentoOrçamentárioCivil" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.SistemaDeGerenciamentoOrçamentárioCivil" parent="Base.Theme.SistemaDeGerenciamentoOrçamentárioCivil" />
</resources>